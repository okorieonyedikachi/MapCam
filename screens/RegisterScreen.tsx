import { View, Text, SafeAreaView } from 'react-native';

const RegisterScreen = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Hello</Text>
      </View>
    </SafeAreaView>
  );
};

export default RegisterScreen;
